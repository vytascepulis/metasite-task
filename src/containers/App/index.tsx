import Header from "../Header";
import Contacts from "../Contacts";
import FilterBar from "../FilterBar";

const App = () => {
  return (
    <main>
      <Header />
      <FilterBar />
      <Contacts />
    </main>
  );
};

export default App;
